import { evaluatePurchaseOrder } from '../lib/purchase-orders-api.js';
export function renderPurchaseOrders(container,{supabase}) {container.innerHTML='<section class="page"><header class="page-header"><div><p class="eyebrow">PURCHASE ORDERS</p><h1>Commercial review</h1><p>Commercial decisions are calculated by the database from saved PO snapshots.</p></div></header><section class="panel"><form id="po-evaluate" class="inline-form"><label>Purchase Order ID<input name="purchaseOrderId" required /></label><button>Evaluate commercial decision</button></form><div id="po-result" role="status"></div></section></section>';container.querySelector('form').addEventListener('submit',async(e)=>{e.preventDefault();const result=container.querySelector('#po-result');try{const decision=await evaluatePurchaseOrder(supabase,new FormData(e.currentTarget).get('purchaseOrderId'));result.innerHTML=`<strong>${decision.decision}</strong><p>${decision.reason}</p><p>Standard FOB: ${decision.standard_fob_usd_mt??'—'} | FOB Equivalent: ${decision.fob_equivalent_usd_mt??'—'}</p>`;}catch(error){result.textContent=error.message;}});}
