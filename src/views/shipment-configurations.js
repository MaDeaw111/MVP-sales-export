import { createShipmentConfiguration,listShipmentConfigurations } from '../lib/shipment-configurations-api.js';
export async function renderShipmentConfigurations(container,{supabase}) { container.innerHTML=`<section class="page"><header class="page-header"><div><p class="eyebrow">SHIPMENT CONFIGURATION</p><h1>Container loads</h1><p>Define valid container, package, bags, and MT combinations.</p></div><button id="new-config">New Configuration</button></header><div id="config-message" role="status"></div><div class="table-wrap"><table><thead><tr><th>Container</th><th>Package</th><th>Bags</th><th>Standard MT</th></tr></thead><tbody id="config-rows"><tr><td colspan="4">Loading configurations…</td></tr></tbody></table></div></section>`; const rows=container.querySelector('#config-rows'),message=container.querySelector('#config-message'); async function load(){try{const configs=await listShipmentConfigurations(supabase);rows.innerHTML=configs.length?configs.map(c=>`<tr><td>${c.container_type}</td><td>${c.package}</td><td>${c.bags_per_container??'—'}</td><td>${c.standard_mt_per_container}</td></tr>`).join(''):'<tr><td colspan="4">No configurations yet.</td></tr>';}catch(error){message.textContent=`Could not load configurations: ${error.message}`;}} container.querySelector('#new-config').addEventListener('click',()=>{message.innerHTML='<form id="config-form" class="inline-form"><label>Container<input name="containerType" required placeholder="20GP" /></label><label>Package<input name="packageName" required /></label><label>Bags<input name="bagsPerContainer" type="number" min="1" /></label><label>Standard MT<input name="standardMt" type="number" step="0.01" required /></label><button>Create configuration</button></form>';message.querySelector('form').addEventListener('submit',async(e)=>{e.preventDefault();try{await createShipmentConfiguration(supabase,Object.fromEntries(new FormData(e.currentTarget)));message.textContent='Shipment configuration created.';await load();}catch(error){message.textContent=error.message;}});});await load(); }
