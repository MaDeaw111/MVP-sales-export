import { createStandardPrice } from '../lib/standard-prices-api.js';
export async function renderStandardPrices(container,{supabase}) { container.innerHTML='<section class="page"><header class="page-header"><div><p class="eyebrow">PRICING</p><h1>Standard FOB Price</h1><p>USD per MT. Default validity is three months from the effective date.</p></div></header><div class="panel"><form id="price-form" class="activity-form"><label>Product ID<input name="productId" required /></label><label>Approved Spec ID<input name="specId" required /></label><label>FOB USD / MT<input name="fobUsdMt" type="number" step="0.0001" required /></label><label>Effective date<input name="effectiveDate" type="date" required /></label><label>Valid until (optional)<input name="validUntil" type="date" /></label><button>Set Standard Price</button></form><div id="price-message" role="status"></div></div></section>'; const message=container.querySelector('#price-message'); container.querySelector('form').addEventListener('submit',async(e)=>{e.preventDefault();try{await createStandardPrice(supabase,Object.fromEntries(new FormData(e.currentTarget)));message.textContent='Standard FOB Price created.';e.currentTarget.reset();}catch(error){message.textContent=error.message;}}); }
