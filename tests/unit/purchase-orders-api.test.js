import { describe, expect, it } from 'vitest';
import { approvePurchaseOrderFx, createPurchaseOrder, evaluatePurchaseOrder, validateShipmentSelection } from '../../src/lib/purchase-orders-api.js';
describe('purchase orders API', () => { it('evaluates a saved PO through the canonical database RPC', async () => { let args; const supabase={rpc:(name,value)=>{args={name,value};return Promise.resolve({data:[{decision:'AUTO_PASS'}],error:null});}}; await expect(evaluatePurchaseOrder(supabase,'po-1')).resolves.toEqual({decision:'AUTO_PASS'}); expect(args).toEqual({name:'evaluate_po_commercial',value:{p_po_id:'po-1'}}); });
it('saves a USD FOB PO with commercial inputs', async () => { let value; const supabase={from:()=>({insert:(input)=>{value=input;return{select:()=>({single:()=>Promise.resolve({data:{id:'po-1'},error:null})})};}})}; await createPurchaseOrder(supabase,{customerId:'c1',number:'PO-1',date:'2026-08-11',productId:'p1',specId:'s1',quantity:100,incoterm:'FOB',destination:'Bangkok',currency:'USD',sellingPrice:500,commission:20}); expect(value).toMatchObject({customer_id:'c1',customer_po_number:'PO-1',product_id:'p1',product_spec_id:'s1',contract_quantity_mt:100,incoterm:'FOB',currency:'USD',final_selling_price:500,commission_usd_mt:20}); });
it('stores the chosen Shipment Configuration in the PO payload', async () => { let value; const supabase={from:()=>({insert:(input)=>{value=input;return{select:()=>({single:()=>Promise.resolve({data:{id:'po-1'},error:null})})};}})}; await createPurchaseOrder(supabase,{customerId:'c1',number:'PO-2',date:'2026-08-11',productId:'p1',specId:'s1',quantity:100,incoterm:'FOB',destination:'Bangkok',currency:'USD',sellingPrice:500,commission:20,shipmentConfigurationId:'cfg-28'}); expect(value.shipment_configuration_id).toBe('cfg-28'); });
it('rejects a fractional Bag 25 kg count', () => { expect(() => validateShipmentSelection({ packageType:'BAG_25KG', bags:'12.5' })).toThrow('Bag 25 kg requires a whole-number bag count'); });
it('sends FX approval only through the manager RPC', async()=>{let args;const supabase={rpc:(name,value)=>{args={name,value};return Promise.resolve({data:'po-1',error:null});}};await approvePurchaseOrderFx(supabase,'po-1',0.92,'WCAT Bank','2026-08-11');expect(args).toEqual({name:'approve_po_fx',value:{p_po_id:'po-1',p_fx_rate:0.92,p_bank_name:'WCAT Bank',p_rate_date:'2026-08-11'}});}); });
